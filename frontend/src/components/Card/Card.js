import React from "react";

const Card = ({ className, size, logo, title, subtitle }) => {
  return (
    <React.Fragment>
      <div className={className}></div>
    </React.Fragment>
  );
};

export default Card;
